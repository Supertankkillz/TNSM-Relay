{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the TNSM Relay GUI: core window/event access, clipboard write (for copying the update download URL), and HTTP fetch for the version-check manifest.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "clipboard-manager:allow-write-text",
    {
      "identifier": "http:default",
      "allow": [
        { "url": "https://*/" },
        { "url": "http://*/" }
      ]
    }
  ]
}
